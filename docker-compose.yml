# Tribal Memory — Docker Compose
#
# Usage:
#   docker compose up
#
# Data persists in ./tribal-memory-data/
# FastEmbed downloads models to the container on first run (~130MB)

services:
  tribal-memory:
    build: .
    ports:
      - "18790:18790"
    volumes:
      - ./tribal-memory-data:/data
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - TRIBAL_MEMORY_INSTANCE_ID=${TRIBAL_MEMORY_INSTANCE_ID:-docker}
    restart: unless-stopped
